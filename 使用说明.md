# mobile-doubao-capture 使用说明

> 豆包 App 参考文献抓取工具 — 一键启动脚本 + 全流程指南

---

## 一、项目简介

跟豆包聊天时，它会引用参考文献（网页链接、来源摘要等）。这套工具自动拦截豆包的 HTTPS 流量，提取每条回答背后的参考文献，按对话框分组保存为结构化 JSON 文件。

**核心能力：**
- 绕过豆包 SSL Pinning，解密 HTTPS 明文
- 提取参考文献（标题、URL、来源、摘要）
- 提取搜索关键词和回答全文
- 按对话框自动分组，同一对话框多次提问自动追加
- 品牌识别 + 来源统计

---

## 二、环境要求

| 依赖 | 说明 |
|------|------|
| Windows / macOS / Linux | 均可 |
| Python 3.10+ | `pip install frida mitmproxy` |
| Android 模拟器 | 推荐 Mumu（ADB 开箱即用） |
| 豆包 App | 模拟器内安装 |
| ADB | [Platform Tools](https://developer.android.com/tools/releases/platform-tools) |
| frida-server | 推入模拟器，版本必须和 `pip install frida` 一致 |

---

## 三、首次配置

### 3.1 克隆项目

```bash
git clone https://github.com/Mxxx9008/mobile-doubao-capture.git
cd mobile-doubao-capture
```

### 3.2 安装 Python 依赖

```bash
pip install frida mitmproxy
```

**确认 frida 版本：**
```bash
pip show frida | grep Version
# → 比如 17.9.8
```

### 3.3 安装 frida-server 到模拟器

```bash
# 1. 连接模拟器
adb connect 127.0.0.1:5555

# 2. 确认架构
adb shell getprop ro.product.cpu.abi
# → arm64-v8a 或 x86_64

# 3. 下载对应版本（版本号必须和上面 pip 的一致！）
# https://github.com/frida/frida/releases
# 文件: frida-server-17.9.8-android-arm64.xz

# 4. 解压并推入
xz -d frida-server-17.9.8-android-arm64.xz
adb push frida-server-17.9.8-android-arm64 /data/local/tmp/frida-server
adb shell chmod 755 /data/local/tmp/frida-server
```

> **这一步只需做一次**，除非换模拟器或更新版本。

---

## 四、一键启动（日常使用）

### 4.1 准备工作

1. 启动 Mumu 模拟器
2. 打开豆包 App（确保在主界面）

### 4.2 运行脚本

```bash
bash start_capture.sh
```

### 4.3 看到就绪提示

```
============================================
   ALL SET - Go chat in Doubao!
============================================

[*] Capture file: /c/Users/.../Desktop/doubao_capture_20260513_143025.mitm
Press Enter when you're done chatting to extract data...
```

### 4.4 提问 & 提取

1. 在豆包里正常输入问题，等待回答完成
2. 可以连续问多个问题
3. 问完后，回到终端按 **Enter**
4. 脚本自动：停止抓包 → 解析数据 → 生成 JSON → 放到桌面

### 4.5 输出

桌面得到 `doubao_conv_{会话ID}.json`，每个对话框一个文件。

---

## 五、输出文件格式

```json
{
  "code": 0,
  "msg": "success",
  "data": {
    "conversation_id": "6834621451355906",
    "updated_at": "2026-05-13T14:30:25.123456",
    "conversations": [
      {
        "task_id": "消息ID",
        "question": "你问豆包的问题",
        "mode": "browsing",
        "search_keywords": ["搜索关键词1", "搜索关键词2"],
        "search_sources": [
          {
            "title": "参考文献标题",
            "url": "https://...",
            "sitename": "来源网站名（知乎、新浪等）",
            "summary": "摘要内容前500字"
          }
        ],
        "search_summary": "搜索 2 个关键词，参考 16 篇资料",
        "answer": "豆包回答的完整文本",
        "total_references": 16,
        "statistics": {
          "sitename_counts": { "知乎": 5, "新浪财经": 3 },
          "brands": ["苹果", "iPhone", "M5"],
          "token_usage": { "total_input_tokens": 0, "total_output_tokens": 0, "total_tokens": 0 }
        }
      }
    ]
  }
}
```

---

## 六、手动操作（分步调试用）

如果一键脚本出问题，可以手动执行每一步排查：

```bash
# 终端 1：frida-server
adb connect 127.0.0.1:5555
adb root
adb shell /data/local/tmp/frida-server &

# 终端 2：mitmproxy
mitmdump -w capture.mitm

# 终端 3：代理 + 注入
adb shell settings put global http_proxy 10.0.2.2:8080
python frida_keepalive.py

# === 在豆包提问 ===

# 提取数据
mitmdump -r capture.mitm --no-http2 > capture.txt
PYTHONIOENCODING=utf-8 python extract_references.py capture.txt
```

---

## 七、项目文件清单

| 文件 | 说明 |
|------|------|
| `start_capture.sh` | **一键启动脚本**（日常只用这个） |
| `frida_keepalive.py` | Frida 注入脚本，持久运行不超时 |
| `frida_ssl_bypass.js` | Frida JS 脚本，Hook SSL 验证逻辑 |
| `frida_inject.py` | Frida 注入器（spawn 方式，备用） |
| `frida_attach.py` | Frida 注入器（包名 attach，备用） |
| `extract_references.py` | SSE 解析管道，原始数据 → JSON |
| `初始使用说明.md` | 新设备从零部署指南 |
| `豆包API逆向抓包-项目总结.md` | 完整技术复盘 |
| `doubao_conv_*.json` | 抓取的对话数据 |

---

## 八、常见问题

### Q: 脚本报 "ADB connection failed"

模拟器没开，或 ADB 端口变了。
```bash
adb connect 127.0.0.1:5555
adb devices  # 确认设备在线
```

### Q: 脚本报 "frida-server failed to start"

frida-server 没 push 或用 `adb root` 失败。
```bash
adb root
adb push frida-server /data/local/tmp/frida-server
adb shell chmod 755 /data/local/tmp/frida-server
```

### Q: 豆包回答里面有参考文献，但 JSON 里是空的

Frida 注入时间太长超时了。重新注入：
```bash
python frida_keepalive.py &
```
然后再提问。

### Q: 中文在终端显示乱码

所有 Python 命令前加 `PYTHONIOENCODING=utf-8`。一键脚本已经自动处理了。

### Q: 模拟器关了之后网络连不上

代理没恢复。手动恢复：
```bash
adb shell settings put global http_proxy :0
```

### Q: 可以在一台新电脑上跑吗

可以。克隆项目 → pip 装依赖 → push frida-server → 跑脚本。详见 `初始使用说明.md`。

---

## 九、快速参考卡片

```
┌─────────────────────────────────────────────────────┐
│                                                     │
│   第一次：clone → pip install → push frida-server   │
│                                                     │
│   每次用：开模拟器 → 开豆包 → bash start_capture.sh │
│           → 聊天 → 按回车 → JSON 到手              │
│                                                     │
│   炸了：adb root → 重装 frida-server → 重试         │
│                                                     │
└─────────────────────────────────────────────────────┘
```
